import React from 'react';
import '../styles/wordButton.css'

const WordButton = ({word}) => {

    return (
        <>
            <button>{word}</button>
        </>
    )
}

export default WordButton;